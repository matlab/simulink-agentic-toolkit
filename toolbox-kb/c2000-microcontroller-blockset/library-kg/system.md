---
type: Simulink Block Category
title: System
description: System-level blocks: watchdog, profiler, and library utilities
tags: [watchdog, profiler, system, timer, reset]
status: stable
source: mathworks_toolbox
library_root: C2000 Microcontroller Blockset
category_path: System
block_count: 52
---

# System

Use these blocks for system.

## Recommended Blocks

| Block | ReferenceBlock | Since | Intent |
|---|---|---|---|
| Blockset Name2 | c2802xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2802xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2803xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2803xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2805xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2805xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2806xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2806xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c280xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c280xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c281xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c281xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2833xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2833xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2834xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2834xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c280013xlib/Blockset Name2 | R2023b+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c280013xlib/Watchdog | R2023b+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c280015xlib/Blockset Name2 | R2023b+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c280015xlib/Watchdog | R2023b+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c28002xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c28002xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c28003xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c28003xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c28004xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c28004xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2807xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2807xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2837xDlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2837xDlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2837xSlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2837xSlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2838xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c2838xlib/Watchdog | R2023a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c2838x_M4_lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | c2838xparentlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M35x_C28x_lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M35x_M3_lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M35xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M36x_C28x_lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M36x_M3_lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | f28M36xlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | c28P55xlib/Blockset Name2 | R2024b+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c28P55xlib/Watchdog | R2024b+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | c28P65xlib/Blockset Name2 | R2024a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Watchdog | c28P65xlib/Watchdog | R2024a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| F29H85x | c29H85xlib/F29H85x | R2025a+ | Device-specific library container for the F29H85x processor family — provides access to peripherals specific to this next-generation C2000 variant |
| Watchdog | c29H85xlib/Watchdog | R2025a+ | Configure the hardware watchdog timer — use to automatically reset the processor if the control loop stalls, preventing uncontrolled actuator states in safety-critical systems |
| Blockset Name2 | tiiqmathlib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | c2000optimizedblks/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Blockset Name2 | c2000lib/Blockset Name2 | R2023a+ | Library identification label displaying the blockset name — internal library element, not used in application models |
| Hardware Profiler | c2000lib/Scheduling/Hardware Profiler | R2024b+ | Measure execution time of code sections using hardware timers — use to verify real-time performance and identify bottlenecks in control loop execution |
